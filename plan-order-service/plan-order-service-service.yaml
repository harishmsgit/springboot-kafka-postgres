apiVersion: v1
kind: Service
metadata:
  name: plan-order-service
spec:
  type: NodePort
  selector:
    app: plan-order-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080

---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
